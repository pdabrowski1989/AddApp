'use strict';

describe('Unit tests for EndscreenComponent', function() {
    beforeEach(module('AddApp'));

    var controller, component;
    beforeEach(function() {
        controller = element.controller('myComponent');
    });
});